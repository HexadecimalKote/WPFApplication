<Window x:Class="WPFApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFApplication"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:col="clr-namespace:System.Collections;assembly=mscorlib"
        mc:Ignorable="d"
        Title="Toolbar" Height="300" Width="700">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <ToolBar Height="25" VerticalAlignment="Top">
            <ToggleButton>
                <Image Source="Resources/comment.png" />
            </ToggleButton>
            <Separator />
            <Button>
                <Image Source="Resources/twitter.png" />
            </Button>
            <Separator />
            <Button>
                <Image Source="Resources/facebook.png" />
            </Button>
            <Separator />
            <Button>
                <Image Source="Resources/telegram.png" />
            </Button>
            <TextBox Foreground="LightGray" Width="100">Поиск...</TextBox>
        </ToolBar>

        <!-- ToolBarTray позволяет создать несколько связанных toolbar-ов -->
        <!-- Orientation="Vertical" - разместить toolbar вертикально -->
        <ToolBarTray Grid.Row="0" Grid.Column="1" Height="25" VerticalAlignment="Top">
            <ToolBar Height="25" VerticalAlignment="Top">
                <ToggleButton>
                    <Image Source="Resources/comment.png" />
                </ToggleButton>
                <Separator />
                <Button>
                    <Image Source="Resources/twitter.png" />
                </Button>
                <Separator />
                <Button>
                    <Image Source="Resources/facebook.png" />
                </Button>
                <Separator />
                <Button>
                    <Image Source="Resources/twitter.png" />
                </Button>
                <TextBox Foreground="LightGray" Width="100">Поиск...</TextBox>
            </ToolBar>
            <ToolBar>
                <Button>
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="10" Height="10" Fill="Black"  HorizontalAlignment="Left"/>
                        <TextBlock HorizontalAlignment="Right" Width="60" Margin="5 0 0 0">Найти</TextBlock>
                    </StackPanel>
                </Button>
            </ToolBar>
        </ToolBarTray>
    </Grid>
    
</Window>